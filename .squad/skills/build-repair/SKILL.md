---
name: build-repair
description: 'When the user says build is broken or requests a repair workflow, explain the build fix path. WHEN: "build is broken", "run build-repair", "fix the build". INVOKES: dotnet restore, dotnet build, dotnet test, markdownlint.'
---
